import * as express from 'express';
// import {
//   contractByRequestId,
//   contractByCustomerSeq,
//   resendContractData,
// } from '../controllers/contract.controller';

const router = express.Router();

// router.get('/:requestId', contractByRequestId);
// router.post('/aplus', contractByCustomerSeq);
// router.post('/context', resendContractData);

export default router;
